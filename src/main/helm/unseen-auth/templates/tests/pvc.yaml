apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unseen-auth-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
